# The source is bind-mounted at run time, so the build context stays tiny.
# Keep large/irrelevant/secret paths out of the daemon context regardless.
.git
.venv
__pycache__
*.py[cod]
.pytest_cache
docs
.DS_Store
secrets
*.key
.env
.env.*
